name: storage_repository
description: Abstraction for reading and persisting data to platform specific storage.
version: 1.1.17+mwd1
repository: https://github.com/salihagic/storage_repository

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  hive: ^4.0.0-dev.2
  isar_flutter_libs: ^4.0.0-dev.14
  flutter_secure_storage: ^10.0.0-beta.4
  path_provider: ^2.1.5
  crypto: ^3.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
